import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import router from '@tanstack/router-plugin'; // 👈 ALTERNATIVA

export default defineConfig({
  plugins: [
    router(), // 👈 ALTERNATIVA
    react(),
  ],
});